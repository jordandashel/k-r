#include <stdio.h>

/* this is a buggy program testing my rudimentary syntax checker in 
 * ex_1-24.c It contains mismatched pairs of brackets, quotes, and 
 * parenthesis. */
main()
{
	char c, arr[i;
	if (c == "quote) {
		this_program_sucks(;
		its_really_terrible);
	}
}
